#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{

    int n=7;
    //scanf("%d", &n);
  	// Complete the code to print the pattern.
      for(int i=0;i<n;i++)
      printf("%d ",n);
      for(i=0;i<n)
      printf
    return 0;
}